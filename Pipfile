[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = "==5.0.0"
django-allauth = ">=0.61.0"
anthropic = ">=0.40.0"
python-decouple = ">=3.8"
requests = ">=2.32.0"
pyjwt = ">=2.12.0"
cryptography = ">=46.0.0"

[dev-packages]

[requires]
python_version = "3.13"
